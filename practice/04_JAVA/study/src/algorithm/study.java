package algorithm;

import java.util.HashMap;
import java.util.Map;

public class study {
    public static void main(String[] args) {
        Map<String, String> in = new HashMap<>();
        Map<String, String> out = new HashMap<>();
        for (Map.Entry<String, String> s: in.entrySet()) {
            if (out.containsKey(s.getValue())) {
                String intime = s.getValue();
//                String outtime = out.
            }
        }

    }
}
